# Unit tests

Las pruebas unitarias actuales viven cerca del código fuente en `src/**/*.test.ts` y `src/**/*.test.tsx`.

Esta carpeta queda reservada si en el futuro se decide centralizar pruebas unitarias.
