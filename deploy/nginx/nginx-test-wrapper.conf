# Archivo mínimo usado únicamente por `nginx -t -c` durante la revisión local.
# Nunca se instala en /etc/nginx.
events {}

http {
    include /etc/nginx/mime.types;
    include /tmp/smartbi-config-under-test.conf;
}
